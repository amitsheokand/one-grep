# dev docs

Process artefacts, not user docs. Users start at the root `README.md`;
everything here is build history.

- `PACKET-ideasearch.md` — eval design note (see also `src/eval.rs`).
- `TASKLIST-fused-rank.md` — retrieves-vs-judges split (still in force).
- `TASKLIST-concept-first.md` — phased concept-first plan and gates.
- `RECEIPT.md`, `RECEIPT-onegrep.md`, `RECEIPT-T-*.md` — per-packet
  delivery receipts: what changed, gates run, numbers observed.
